{{ 'T2273_content.scss.css' | asset_url | stylesheet_tag }}

{% assign custom_css = section.settings.custom_css %}

{% style %}
  {%- if custom_css != blank -%}
  {{ custom_css }}
  {% endif %}
{% endstyle %}

{% schema %}
{
  "name": "T2273 ",
  "presets": [ 
    {
      "name": "T2273 content"
    }
  ],
  "settings": [{
    "type": "textarea",
    "id": "custom_css",
    "label": "Custom CSS",
    "info": "自定义样式（注意：请按照标准的css语法添加样式）"
    }
  ]
}
{% endschema %}